/* ============================================
   HOMEPAGE CUSTOM STYLES
   Estilos centralizados para secciones del homepage
   ============================================ */

/* --- Fuentes --- */
@font-face {
  font-family: 'DIN-Light';
  src: url('{{ "DIN-Light.otf" | asset_url }}') format('opentype');
  font-weight: 300;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: 'Galgo';
  src: url('{{ "Galgo.otf" | asset_url }}') format('opentype');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: 'Chivo-Medium';
  src: url('{{ "Chivo-Medium.ttf" | asset_url }}') format('truetype');
  font-weight: 500;
  font-style: normal;
  font-display: swap;
}

@font-face {
  font-family: 'D-DIN';
  src: url('{{ "D-DIN.otf" | asset_url }}') format('opentype');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}

/* ============================================
   HERO BANNER
   ============================================ */

/* Contenedor general que fuerza ancho completo */
.hero-banner-wrapper {
  width: 100vw;
  margin-left: calc(-50vw + 50%);
  overflow: hidden;
}

/* Banner principal */
.hero-banner {
  position: relative;
  width: 100%;
  background-size: cover;
  background-position: center center;
  background-repeat: no-repeat;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  color: #fff;
}

/* Contenedor del contenido - siempre centrado */
.hero-content {
  z-index: 2;
  padding: 0 20px;
  text-align: center;
  width: 100%;
  max-width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

/* Texto principal/subtitle - siempre centrado */
.hero-subtitle {
  font-family: 'DIN-Light', 'D-DIN', sans-serif;
  font-size: clamp(20px, 3vw, 26px);
  font-style: normal;
  font-weight: 300;
  margin-bottom: 25px;
  letter-spacing: 0.5px;
  color: #fff;
  text-align: center;
  width: 100%;
}

/* Botón - siempre centrado */
.hero-button {
  display: inline-block;
  border: 1px solid #fff;
  padding: 10px 24px;
  font-size: 12px;
  font-family: 'D-DIN', sans-serif;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: #fff;
  text-decoration: none;
  transition: all 0.3s ease;
  cursor: pointer;
  text-align: center;
  margin: 0 auto;
}

.hero-button:hover {
  background-color: #fff;
  color: #000;
}

/* ============================================
   RESPONSIVE - HERO BANNER
   ============================================ */

@media (max-width: 768px) {
  .hero-banner {
    background-position: center center;
  }

  .hero-content {
    text-align: center;
    align-items: center;
  }

  .hero-subtitle {
    font-size: 20px;
    margin-bottom: 20px;
    text-align: center;
  }

  .hero-button {
    padding: 9px 22px;
    font-size: 11px;
    text-align: center;
    margin: 0 auto;
  }
}

@media (max-width: 480px) {
  .hero-banner {
    background-position: center center;
  }

  .hero-content {
    text-align: center;
    align-items: center;
    padding: 0 15px;
  }

  .hero-subtitle {
    font-size: 18px;
    text-align: center;
  }

  .hero-button {
    text-align: center;
    margin: 0 auto;
  }
}

/* ============================================
   SERVICES ICONS
   ============================================ */

.custom-services {
  background-color: var(--services-bg-color, #F1F2F2);
  padding-top: var(--services-padding-top, 80px);
  padding-bottom: var(--services-padding-bottom, 80px);
  text-align: center;
}

.services-container {
  display: grid;
  grid-template-columns: repeat(var(--services-columns-desktop, 3), minmax(0, 1fr));
  gap: var(--services-gap, 80px);
  justify-items: center;
  align-items: stretch;
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 40px;
}

/* Carrusel - oculto por defecto (solo visible en mobile) */
.services-carousel {
  display: none;
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 20px;
}

.service-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  transition: transform 0.3s ease, opacity 0.3s ease;
}

.service-item:hover {
  transform: translateY(-6px);
  opacity: 0.9;
}

.service-icon-wrapper {
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 20px;
}

.service-icon {
  width: 60px;
  height: auto;
}

.service-item h3 {
  font-family: 'D-DIN', sans-serif;
  font-size: 18px;
  font-weight: 700;
  letter-spacing: 1px;
  color: #231f20;
  text-transform: uppercase;
  margin: 0;
}

@media (max-width: 992px) {
  .services-container {
    grid-template-columns: repeat(var(--services-columns-tablet, 2), minmax(0, 1fr));
    gap: calc(var(--services-gap, 80px) * 0.75);
    padding: 0 30px;
  }

  .service-icon-wrapper {
    height: 70px;
  }
}

@media (max-width: 600px) {
  .custom-services {
    padding-top: calc(var(--services-padding-top, 80px) * 0.75);
    padding-bottom: calc(var(--services-padding-bottom, 80px) * 0.75);
  }

  /* Ocultar grid en mobile */
  .services-container {
    display: none !important;
  }

  /* Mostrar carrusel en mobile */
  .services-carousel {
    display: block;
  }

  .services-carousel .service-item {
    padding: 0 10px;
  }

  .service-icon-wrapper {
    height: 60px;
    margin-bottom: 15px;
  }

  .service-item h3 {
    font-size: 13px;
    letter-spacing: 0.8px;
  }
}

/* ============================================
   GET INSPIRED
   ============================================ */

.get-inspired {
  background-color: #000;
  text-align: center;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.get-inspired-title {
  font-family: 'Galgo', sans-serif;
  font-size: clamp(40px, 6vw, 80px);
  color: #fff;
  letter-spacing: 2px;
  text-transform: uppercase;
  line-height: 1.1;
  margin: 0;
  padding: 0;
}

.get-inspired-grid {
  display: grid;
  max-width: 1100px;
  margin: 0 auto;
  padding: 60px 30px 0;
}

/* Carrusel - oculto por defecto (solo visible en mobile) */
.get-inspired-carousel {
  display: none;
  max-width: 1100px;
  margin: 0 auto;
  padding: 40px 20px 0;
}

.inspired-item {
  overflow: hidden;
  border-radius: 2px;
  transition: transform 0.4s ease;
}

.inspired-item img {
  width: 100%;
  height: auto;
  aspect-ratio: 3 / 4;
  object-fit: cover;
  transition: transform 0.4s ease;
}

.inspired-item:hover img {
  transform: scale(1.05);
}

.inspired-placeholder {
  width: 100%;
  aspect-ratio: 3 / 4;
  background-color: rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 2px;
  border: 1px dashed rgba(255, 255, 255, 0.3);
}

.inspired-placeholder p {
  color: rgba(255, 255, 255, 0.5);
  font-family: 'D-DIN', sans-serif;
  font-size: 14px;
  text-align: center;
  padding: 20px;
}

.get-inspired-empty {
  max-width: 1100px;
  margin: 60px auto 0;
  padding: 40px 20px;
  text-align: center;
}

.get-inspired-empty p {
  color: rgba(255, 255, 255, 0.6);
  font-family: 'D-DIN', sans-serif;
  font-size: 16px;
}

/* Estilos para el carrusel Slick */
.get-inspired-carousel .slick-slide {
  padding: 0 10px;
}

.get-inspired-carousel .slick-dots {
  bottom: -40px;
}

.get-inspired-carousel .slick-dots li button:before {
  color: #fff;
  font-size: 12px;
  opacity: 0.5;
}

.get-inspired-carousel .slick-dots li.slick-active button:before {
  opacity: 1;
}

.get-inspired-carousel .slick-prev,
.get-inspired-carousel .slick-next {
  z-index: 2;
  width: 40px;
  height: 40px;
}

.get-inspired-carousel .slick-prev:before,
.get-inspired-carousel .slick-next:before {
  font-size: 30px;
  color: #fff;
  opacity: 0.8;
}

.get-inspired-carousel .slick-prev {
  left: 10px;
}

.get-inspired-carousel .slick-next {
  right: 10px;
}

.get-inspired-carousel .slick-prev:hover:before,
.get-inspired-carousel .slick-next:hover:before {
  opacity: 1;
}

@media (max-width: 992px) {
  .get-inspired {
    padding-top: 80px;
    padding-bottom: 80px;
  }

  .get-inspired-grid {
    grid-template-columns: repeat(var(--inspired-columns-tablet, 2), 1fr);
    gap: 20px;
    padding: 50px 20px 0;
  }
}

@media (max-width: 600px) {
  .get-inspired {
    padding-top: 60px;
    padding-bottom: 60px;
  }

  .get-inspired-title {
    font-size: 40px;
    margin-bottom: 0;
  }

  /* Ocultar grid en mobile */
  .get-inspired-grid {
    display: none !important;
  }

  /* Mostrar carrusel en mobile */
  .get-inspired-carousel {
    display: block;
  }

  .get-inspired-carousel .inspired-item img {
    aspect-ratio: 4 / 5;
  }
}

/* ============================================
   ABOUT DESCONTINUADO
   ============================================ */

.about-descontinuado {
  width: 100%;
  background-color: #fff;
  color: #000;
}

.about-container {
  display: flex;
  flex-wrap: wrap;
  width: 100%;
  min-height: 80vh;
}

.about-text {
  flex: 1 1 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 80px 100px;
}

.about-text h2 {
  font-family: 'Chivo-Medium', 'Chivo', sans-serif !important;
  font-size: 32px !important;
  font-weight: 500 !important;
  line-height: 1.3 !important;
  letter-spacing: 0.5px !important;
  text-transform: uppercase !important;
  margin-bottom: 25px !important;
  white-space: pre-line !important;
  color: #000 !important;
}

.about-text p {
  font-family: 'D-DIN', 'DIN', sans-serif !important;
  font-size: 15px !important;
  line-height: 1.7 !important;
  max-width: 440px !important;
  margin-bottom: 35px !important;
  color: #222 !important;
}

.about-button {
  display: inline-block !important;
  font-family: 'D-DIN', 'DIN', sans-serif !important;
  border: 1px solid #000 !important;
  padding: 10px 24px !important;
  font-size: 12px !important;
  font-weight: 700 !important;
  letter-spacing: 1px !important;
  text-transform: uppercase !important;
  color: #000 !important;
  text-decoration: none !important;
  transition: all 0.3s ease !important;
  cursor: pointer !important;
  width: auto !important;
  max-width: 200px !important;
  text-align: center !important;
  background-color: transparent !important;
}

.about-button:hover {
  background-color: #000 !important;
  color: #fff !important;
}

.about-image {
  flex: 1 1 50%;
}

.about-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

@media (max-width: 1024px) {
  .about-text {
    padding: 60px 70px;
  }

  .about-text h2 {
    font-size: 28px !important;
    font-family: 'Chivo-Medium', 'Chivo', sans-serif !important;
  }
}

@media (max-width: 768px) {
  .about-container {
    flex-direction: column;
  }

  .about-text {
    padding: 60px 40px;
    text-align: center;
  }

  .about-text h2 {
    font-family: 'Chivo-Medium', 'Chivo', sans-serif !important;
    text-align: center !important;
  }

  .about-text p {
    margin-left: auto;
    margin-right: auto;
    font-family: 'D-DIN', 'DIN', sans-serif !important;
  }

  .about-button {
    margin: 20px auto 0 !important;
    font-family: 'D-DIN', 'DIN', sans-serif !important;
  }
}

@media (max-width: 480px) {
  .about-text {
    padding: 50px 30px;
  }

  .about-text h2 {
    font-size: 24px !important;
    font-family: 'Chivo-Medium', 'Chivo', sans-serif !important;
  }

  .about-text p {
    font-size: 14px !important;
    font-family: 'D-DIN', 'DIN', sans-serif !important;
  }

  .about-button {
    font-family: 'D-DIN', 'DIN', sans-serif !important;
  }
}

